# Copy to .env.local (Next.js convention). Never commit .env.local.

# Backend API base URL. The frontend NEVER calls the LLM or external
# data APIs directly — only this backend, which is the hard architectural
# boundary described in TDD Section 1.
NEXT_PUBLIC_API_URL=http://localhost:8000
